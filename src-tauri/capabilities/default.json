{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. O updater recebe check/download/install separados de propósito: `allow-download-and-install` instalaria no meio do uso e, no Windows, encerraria o app sozinho — a instalação só pode acontecer quando a pessoa clicar em 'Reiniciar agora' (V7/F16).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "updater:allow-check",
    "updater:allow-download",
    "updater:allow-install",
    "process:allow-restart"
  ]
}
